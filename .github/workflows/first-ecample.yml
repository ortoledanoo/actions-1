name: My First Workflow

on: push

jobs:
    first_job:
        runs_on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My First GitHub Actions"

            - name: List files
              run: ls
            
            - name: Read file
              cat: README.md
